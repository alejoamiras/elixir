# Phase 5 lessons — proverless, conditional, last (arc D)

**Why it is built**: P1 measured browser transaction proving at 30.3% of test time in CI (27.3% of the e2e step), against the 15% bar. The owner's position stands: real proving survives in one place, the canary, and need not run everywhere.

**Gate (plan, P5)**: `bun run lint` · `bun test packages/site` (the flag dropped in production, asserted through `viteDefine`) · a flagged build proving the marker present and a production build proving it absent · the negative canary failing for the specified reason, demonstrated by showing it passes when the input is untampered · the claim lane green with its measured saving. Result: see **Evidence**.

## What was built

- **The flag** follows the `config.ts` template: `proverless: mode === 'e2e' && env.VITE_E2E_PROVERLESS === '1'`, emitted as `VITE_E2E_PROVERLESS`; production construction discards the environment, so it is `''` there by the same mechanism as the other e2e hooks. `wallet.ts` reads it once (`PROVERLESS`) and passes `proverEnabled: !PROVERLESS` to the embedded PXE — the one place the page proves transactions, so the flag fakes every transaction the wallet makes (claims, withdrawals, rolls), as the plan said it would.
- **The marker is inseparable from the flag**: `PROVERLESS_MARKER` (`yacana:proverless`, exported from `config.ts`) is only ever referenced inside `if (PROVERLESS) console.warn(...)`. Under the production define the condition folds to `false` and the branch — string included — is dropped; a flagged build keeps it. `tests/proverless-marker.bun.test.ts` runs three real Vite builds into temp dirs (flagged e2e: marker present; unflagged e2e and production: absent) — about 6 s in all. Arc A's artifact inspector (`artifact.ts`) now also fails a production assembly whose scripts carry the marker, and refuses a resolved `proverless` config.
- **The canary** (`e2e/canary.e2e.ts`, its own shard `canary`, the only one CI runs without `E2E_PROVERLESS`): boots on the real-proving build, asserts `window.yacana.proverless === false`, arms `tamperNextClaim()`, mines at the easy target. The controller flips the lowest bit of the winning ticket's `out` before `sendClaim`: the ticket digest (over the proof fields), the epoch and the nullifier stay valid, and `out` is used by the claim circuit only as a public input of `verify_honk_proof_non_zk` — which the ACVM treats as a black box, so simulation passes and the claim fails at ClientIVC proving. The spec requires the failure's kind `other`, a prover's message, none of the other explanations (`epoch is not open`, `ticket above target`, reverted, expired), zero claims — then presses Start again and requires the untampered claim to mint (the positive control). A proverless build would send the tampered claim for the local network's `TestCircuitVerifier` to mint, which is why this shard alone keeps the prover.
- **The meter under proverless**: `proofShortfall(title, meter, proverless)` ignores the floors and fails any test that produced a proof event at all — a build meant to skip proving that proved is as wrong as a real build that did not.
- **CI**: `e2e.yml` sets `E2E_PROVERLESS: ${{ matrix.shard != 'canary' && '1' || '' }}`; `run-setup.ts` forwards it into the build as `VITE_E2E_PROVERLESS`. The inventory test holds sharded (17) + moved (3) = 19 + the canary.

## Codex fix loop (arc D)

- 2026-09-10 · Round 1 (GPT-6 Astra, high, session in `scratchpad/auditd`): **changes**, five findings. (1) The lost-race fix's first shape still raced: the warm burst was started when the page's first claim landed and awaited *inside* the held request, so a fast next win (easy target) put all three claims under the hold — the local run's own timestamps showed it (hold 20:12:41, warm claims 20:13:05 and 20:13:22, closer 20:13:41). Now the page is stopped, the warm-up runs to completion, then Start. (2) "Warm two" assumed the epoch held exactly the page's claim; an unsharded run's earlier specs claim in the same epoch, so the count now comes from the tile and the warm-up targets `N − 1` total. (3) The canary's `/prov|verif|ClientIVC|circuit/` accepted a simulation failure (`Circuit execution failed`) and a node validation error; the message is now pinned to the PXE's exact text `Failed to verify the generated proof!` (found by running it — `/prov/` does not match "proof") and the spec requires zero `aztec_sendTx` before the refusal. (4) The positive control mines a fresh ticket, not the same one with `out` restored: retrying the identical ticket needs a reducer transition that does not exist (`winner` is discarded from `idle`), which would be product code for the test; the title now says what is shown. (5) Unit cases for `proofShortfall(…, proverless)` and for the flag's `e2e`-only binding. Codex confirmed: no production bypass on the supported routes; the marker log's removal fails the flagged-build test; the tamper hook adds no authority beyond existing console access; an accidentally flagged canary fails its first assertion.

- 2026-09-10 · Round 2 (resumed): **changes**, two small items — the controller's poll keeps running while mining is stopped (only a switch or a recovery pauses it), so the `N − 1 of N` check belongs before Start, while the epoch is stable, not after it when the closer may already have advanced the epoch; and one stale "same ticket" comment. Codex also read the regrouping as coverage-preserving and the fresh-ticket control as acceptable with the corrected title. Both applied.
- 2026-09-10 · Round 3 (resumed): **approve** — "Both findings are resolved. The assertion now checks a stable epoch before mining resumes, and the comment accurately describes the canary. No further changes requested." Converged in three rounds.

## The ledger P5 actually moved

The first CI run of this arc (34525290532, four shards) was green everywhere, and it showed what the plan's "measured saving" really is. Proverless cut the three shards from 13.2 / 12.7 / 14.7 min to 12.8 / 11.2 / 13.7 (every test 0 proof events, as the meter demands; test time 1624 → 1181 s across the three), but the canary's own job cost 8.6 min, so the matrix went from 40.6 to **46.3 runner-minutes** — over P2's 45 — for a wall clock of 13.7 instead of 14.7. The proving that left the three shards was smaller than one more rig. What proverless buys is therefore not minutes on the same layout; it is room to run fewer jobs: the canary's job has capacity for the specs that never prove on either build (words, opening), and the three remaining proverless files fit in two shards. Regrouped: `canary` = canary + words + opening, `cockpit` = miner + passkey, `chain` = states + switch + withdraw + presto — three jobs. The regrouped run's numbers are the phase's evidence below.

## Evidence

- **Flag and marker**: `bun test packages/site` green (the define is `"1"` under e2e with the flag, `""` in production even with the flag in the environment; a flagged `dev` config is not proverless); `tests/proverless-marker.bun.test.ts` — three real builds, marker present in the flagged e2e bundle, absent in the unflagged e2e and the production bundle; `artifact.test.ts` — a script carrying the marker fails the production inspection, a resolved `proverless` config is refused.
- **The canary locally** (real prover): `[canary] claim failed (other): Failed to verify the generated proof!` — the PXE's own text when it verifies the proof it just generated; zero `aztec_sendTx` before it; then the next claim mints with one proof event (24 s locally, 45 s in CI); 74 s of test.
- **A proverless shard locally** (`E2E_PROVERLESS=1 E2E_SHARD=chain`): 4 tests green, 0 proof events; the lost race 128 s with a 31 s hold (the closer alone), the outside miner's log showing two warm claims land before the page resumes and one closer under the hold.
- **CI, first layout** (34525290532): canary 8 min 38 s (1 proof, 45.3 s), cockpit 12 min 50 s, accounts 11 min 11 s, chain 13 min 40 s, all proverless shards at 0 proof events; merge green.
- **CI, regrouped** (34527495372, three jobs): canary 9 min 8 s (canary + opening + words; 1 proof event, 46.0 s; 270 s of tests), chain 10 min 19 s (states + switch + withdraw + presto, 393 s of tests, 0 proof events), cockpit 14 min 9 s (miner + passkey, 515 s of tests, 0 proof events); merge green, every one of the 20 tests executed. **33.6 runner-minutes (was 40.6 before P5, budget 45); slowest job 14.2 min (was 14.7, budget 15).** The lost race took 137 s with the warm-up in place. Runner variance is real: the same cockpit files took 417 s of tests in the previous run and 515 s here (the CRS spec 40 → 66 s, the power test 137 → 161 s), which is why the ceiling keeps a margin rather than being tightened.

## After the approve: what the owner asked for to raise confidence (2026-09-10, before the merge)

- **The canary's control is the same claim.** The reducer gained a `retry` event (idle → claiming with the `submit` command; a no-op from any other phase); `controller.submit()` keeps a tampered claim's ticket and restores `pending` when it fails; `retryPendingClaim()` dispatches the retry. The canary now resubmits the identical claim with only `out` restored and requires the mint with exactly one `aztec_sendTx`. Locally: refused (`Failed to verify the generated proof!`), then minted, 68 s of test, one proof event. Codex's round-1 point about the control is now met as specified.
- **The transfers prove for real again.** `withdraw.e2e.ts` moved into the canary shard, closing the one flow the proverless split had left unproved anywhere; locally its three proofs ran on the real prover in that shard (5.5 min for the four files).
- **The marker chain on real assemblies**: a production `site:build` with `VITE_E2E_PROVERLESS=1` in the environment succeeded with zero marker hits in `dist`; an e2e assembly with the flag carried the marker in one chunk; that assembly with `build.json` and every `_headers` forged to production was refused by the inspector with `mine/assets/index-….js carries the proverless branch` — the marker check itself, on a real bundle, not the unit fixture.
- **Codex on the follow-up** (resumed session): round 1 **changes** — `pending` did not exclusively identify a retained tampered failure: a winner arriving after Stop fills it before the reducer ignores the winner, so the hook could have resubmitted a stopped ticket; and after Start the stale entry survived. Fixed with a field of its own (`retained`), cleared when a new job starts, behind a pure `retryEligible(retained, secrets, phase)` that needs an idle page and the claim's secret still current — unit-tested for both sequences. Round 2 **approve**.
- **Five sharded runs, dispatched together** (34534192202, 34534211380, 34534231914, 34534254669, 34534275463 on `94e32ac`): **5 of 5 green**, the merge job's coverage line on each. Shard clocks (canary / chain / cockpit, seconds): 710 / 842 / 718 · 725 / 783 / 730 · 739 / 766 / 689 · 746 / 824 / 772 · 427 / 610 / 530. The first four ran fifteen shard jobs at once and still kept every job under 14.1 min (37–39 runner-minutes each); the fifth, alone once the others drained, took 10.2 min at its slowest and 26 runner-minutes — the figure a lone dispatch will see. The lost race passed in all five (and in the two earlier regrouped runs): 7 of 7 since the warm-up fix, against 1 of 3 before it.
- **A sixth run on the final tip** (`4128be0`, 34534886334, alone): green — canary 605 s, cockpit 638 s, chain 726 s; 32.8 runner-minutes, slowest 12.1 min. Merged on this.

## What P5 delivered, in one line

The suite runs in three jobs instead of four, 33.6 runner-minutes instead of 40.6, with the same wall clock and one shard that still proves for real and shows what a fake proof does.

## The cross-arc pass (fresh codex session over `main..e2e-lanes-proverless`)

- 2026-09-10 · Round 1: **changes** — one coverage gap and cleanup. (1) `web-stats.yml`'s filter did not include `scripts/run/**`, where arc C moved the preview helpers the stats lane now imports; added. (2) `run-setup.ts` kept its own `localhost`-only readiness probe while the replay and stats lanes used the shared one that tries both loopback families; the shared probe now serves all three. (3) The replay fixture's `served()` had no consumer once the poll test observed the changed claims value; removed with two unused imports (`Page` in the fixture, `BOOT_MS` in `presto.e2e.ts`) that Biome does not flag. (4) Documentation describing an earlier shape: CLAUDE.md and the setup header called the e2e bundle a "production build" (it is a Vite build in e2e mode, and can now disable proving); `config.ts` said production "disables the e2e hooks" (it discards the build-time overrides; `window.yacana` stays); the inventory comment still said nineteen; `docs/deployments.md` and the inspector's docblock lacked the marker check; the fixture header said workers are "refused" (they are recorded as failures). (5) Comment trims in the workflow header, the canary header, the dialog spec's step narration and the poll comment. Codex's security read of the whole: the flag → wallet → marker → inspector chain holds on every supported assembly route, the bare `wrangler deploy` of existing assets remains the documented exception, and the hooks add no authority beyond the session and controller the page already exposes. All applied in one commit on the top of the stack (`cb06d7e`).
- 2026-09-10 · Round 2 (resumed): **approve** — "All five findings are resolved. No new issues in the follow-up diff."
