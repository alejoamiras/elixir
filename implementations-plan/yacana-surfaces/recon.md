# Recon — yacana-surfaces

Read-only survey of the clean `main` base (f2bf7a6) before drafting. Two agents: a reuse sweep over the capabilities the plan needs, and a rename inventory for arc 0. Every "build new" carries its justification; every absence claim carries its search trail.

## Reuse sweep

## Reuse map

| capability | existing code | verdict | note |
|---|---|---|---|
| Node-only chain reads (epochs, claims, targets, opened_at, open_epoch) | `packages/deploy/scripts/epoch-stats.ts` (`epochStats(nodeUrl, miner)`, Bun CLI shape); `packages/web-miner/src/chain.ts` `crossCheck` (same primitives, already in the browser bundle, exercised in `e2e/miner.e2e.ts:132`) | **adapt** → extract one DOM-free module in `packages/miner-core` (`readEpochsFromStorage(node, miner, layout)`, `readTotalSupply(node, token, layout)`) | the packed `EpochParams = [target, seed, opened_at]` decoding is already duplicated in two places; a third copy is the dedup risk. `deriveStorageSlotInMap` (`@aztec/stdlib/hash`) is proven browser-safe. `readOpenEpoch`/`readRules`/`readBalance` need a wallet (`.simulate({from})`). Token `total_supply`: no helper exists; use `getPublicStorageAt('latest', token, layout.total_supply.slot)`, not `live.test.ts`'s `computePublicDataTreeLeafSlot`. |
| Wallet / account | `packages/web-miner/src/wallet.ts` `openWallet()` (EmbeddedWallet, two IndexedDB stores namespaced `${chainId}-${rollupVersion}-${rollupAddress}`, SponsoredFPC, one initializerless Schnorr account) | **adapt** (key derivation becomes an input: passkey PRF master or bip39 seed → secret/salt/signing key) | no encryption at rest, no mnemonic/passkey/WebAuthn anywhere (grep: 0 hits). The `Fr.random() → createSchnorrInitializerlessAccount(secret, salt, deriveMasterMessageSigningSecretKey(secret))` triple is duplicated at 8 call sites. Rotation exists only in `deploy/scripts/soak.ts` (`rotateAccount`, `isDeliveryBlockedError`); the web miner today stops instead. Stats/landing need NO wallet module. |
| Token interactions | `miner-core/src/claim.ts` (`claimGasLimits`, `buildClaim`, `isDeliveryBlockedError`, `DELIVERY_BLOCKED_MESSAGE`); `web-miner/src/chain.ts` (`sendClaim`, `sendRoll`, `readBalance` = the only balance read) | claim/balance **reuse-as-is**; withdraw **build new** | zero hits for `transfer`, `transfer_private_to_public`, `balance_of_public` anywhere. Artifact loading has two strategies: browser `fetch('/artifacts/token_contract-Token.json')` + `loadContractArtifact` (via `scripts/copy-artifacts.ts`); Node scripts import `TokenContract` from `@aztec-foundation/aztec-standards/artifacts/src/artifacts/Token.js`. |
| Prover worker + mining loop | `web-miner/src/prover.worker.ts` (continuous until-winner loop, queueing), `worker-protocol.ts`, `controller.ts` (crash generations, secret rotation, nonce resumption), `lib/reducer.ts` (pure state machine); `miner-core/src/miner.ts` `mineEpoch`, `miner-core/src/work.ts` `BbJsWorkProver.prove()` (single-proof primitive); `pinned-crs.ts` (`preloadPinnedCrs`, `purgeCrsCache`, fetch interceptor keyed on `crs.lock.json`, must be imported first in every Barretenberg context); `shims/node-globals.ts` (must be the first import of any Worker entry) | mining **reuse-as-is**; demo **build new (small)**: a `proveOnce` message on a trimmed worker entry reusing `BbJsWorkProver`, the shims and the CRS interceptor | the demo needs COOP/COEP, `/artifacts/elixir_work.json`, `/crs/*`; `scripts/{copy-artifacts,fetch-crs}.ts` are web-miner-local and hardcode relative paths into sibling packages; `crs.lock.json` must stay a single source of truth. |
| UI layer | shadcn primitives in `web-miner/src/components/ui/*` (`radix-nova` style, `data-slot` convention, `cn()` in `lib/utils.ts`), `src/index.css` (Tailwind v4 `@theme inline` tokens, oklch palette, `@fontsource-variable/geist`), `components.json`, hand-rolled `theme-provider.tsx` | primitives/tokens/font strategy **reuse-as-is** into `packages/ui`; theming **adapt** | collision: `theme-provider.tsx` is never mounted, and `sonner.tsx` imports `useTheme` from `next-themes` whose provider is never mounted either → pick one. `separator.tsx` is dead. `vite.config.ts` is bb.js-specific (nodePolyfills, dedupe, optimizeDeps, `productionHeaders()` parsing `_headers` for dev/preview parity); stats/landing need a plain config unless they embed the prover. |
| Metrics / format | `web-miner/src/lib/format.ts` (`difficulty`, `compact`, `expectedSecondsToWin`, `duration`, `amount`, `shortAddress`), `reducer.ts` `proofsPerSecond`, `miner-core/src/retarget.ts` (`nextTarget`, `cappedElapsed`, `nextSeed`, `EpochRules`) | **reuse-as-is** | `nextTarget` is used only by tests/`pin-vectors.ts` today; the "if it closed now" tile is its first UI consumer. Mirrored-from-Noir math follows the generated-vector test pattern (`vectors.test.ts`). |
| Test infra | `bunfig.toml` preload `scripts/test-preload.ts`; Vitest (`vitest.config.ts`, jsdom, `tests/setup.ts`, colocated `src/**/*.test.ts(x)`, `csp.test.ts` cross-checks `_headers` vs `.env.production`); Playwright (`testMatch: /.*\.e2e\.ts$/`, Bun-side `e2e/run-setup.ts` + `run-teardown.ts` behind Node `global-setup.ts`; `E2E_SERVER=preview|dev`; port claimed on lane 6 via `scripts/run/registry.ts` + `port-window.ts`; throwaway deployment via `deployElixir` at `ELIXIR_E2E_TARGET`; `.run.json` shape in `e2e/run.ts`; page pinned by `?node=&miner=&token=`) | **reuse-as-is** (mechanism); new packages **build new** setup scripts on the same pattern; a shared helper once a second consumer exists | no virtual-authenticator/CDP usage (grep: 0). Lanes 4/5/7+ are free. |
| CI | 6 workflows; 5 share the verbatim `changes` job (dorny/paths-filter + manual-dispatch override) gating `test`; `.github/actions/setup-bun` (bun 1.4.0, frozen lockfile), `setup-aztec`; `actionlint.yml`; SHA-pinned `uses:`; `permissions: contents: read` everywhere; **no secrets used**, **no E2E in CI**, no `wrangler` in CI | **reuse-as-is** (template per package) | filters enumerate upstream deps ("what could break me"); each includes its own workflow file + `.github/actions/**`. Pre-existing debt: the `changes` job is copy-pasted 5×. |
| Cloudflare / deploy | `web-miner/wrangler.jsonc` (`elixir-web-miner`, `pages_build_output_dir: dist`, manual deploy); `public/_headers` with the origin-wide `/*` block (COOP/COEP, CSP `connect-src 'self' data: <node>`); `deployments/testnet.json` (flat public record: addresses, class ids, salts, rollupVersion, params, launch times); `.env.production` `VITE_*` public values via `config.ts` | headers/record **reuse-as-is**; topology **decide**: one assembled `dist` behind one `wrangler.jsonc` vs three projects | `_headers` supports per-path blocks; `csp.test.ts` must follow whatever shape results. |
| Run isolation | `scripts/run/{registry.ts, port-window.ts, isolated-node.ts, agent.sh}` (generic, package-agnostic) | **reuse-as-is** | `resolve-ports.ts` does not exist (0 hits). Root `test:components` hardcodes `./packages/web-miner`; `test:e2e` is already a glob. |
| Docs | `CLAUDE.md` package table, `docs/roadmap.md`, `docs/threat-model.md` ("Web page", "Lying RPC" rows), `implementations-plan/index.md` | **adapt** | add rows per package; threat-model rows for stats/landing chain reads and the demo Worker. |

## Cross-cutting

- `docs/pitch/elixir-pitch.html` and `implementations-plan/elixir-core/eli5.html` are hand-rolled static pages (IBM Plex, own CSS), reference only.
- Dead code to decide on while extracting `packages/ui`: `theme-provider.tsx` (unmounted) vs `next-themes` (provider never mounted), `separator.tsx`.
- Every absence claim above was verified by an explicit grep/find with zero matches (mnemonic/bip39/passkey/WebAuthn; transfer/balance_of_public; CORS handling; CI e2e; CI secrets; virtual authenticator/CDP; `resolve-ports`).


## Recon · rename inventory Elixir → Yacana (read-only, clean `main` at f2bf7a6)

## 1. Protocol-level (regenerates crypto artifacts, needs a new deployment)

- `elixir.params.json`: 7 domain tags (`ELX/depl ELX/secr ELX/work ELX/tick ELX/null ELX/seed ELX/lnch`, lines 5–11); `GENESIS_SEED` hex decodes to `ELX/testnet` (line 24) and `ELX/mainnet` (line 40); `TOKEN_NAME/SYMBOL` "Elixir Testnet"/"tELX" (27–28) and "Elixir"/"ELX" (43–44); `$comment` mentions `ELIXIR_PROFILE`.
- `scripts/params-codegen.ts` (reads `ELIXIR_PROFILE`, line 13; enforces ≤ 31-byte tags) writes: `packages/contracts/elixir_miner/src/params.nr`, `packages/work-circuit/crates/elixir_work/src/params.nr`, `packages/work-circuit/crates/lib/src/domains.nr` (7 `DOM_*` fields), `packages/miner-core/src/generated/params.ts` (PARAMS incl. TOKEN_NAME/SYMBOL + DOMAINS), `packages/contracts/elixir_miner/src/test/vectors.nr` (from `fixtures/vectors.json` when present).
- **`DOM_WORK` is hashed inside the work circuit** (`packages/work-circuit/crates/lib/src/lib.nr:11`: `Poseidon2::hash([DOM_WORK, domain, seed, epoch, miner_commit, nonce], 6)`), so a tag change changes the `elixir_work` ACIR → new VK and `W_VK_HASH`, and the committed fixture proof no longer matches the circuit. `DOM_TICKET` is hashed in `ticket.nr`; the other five in `main.nr`/`launch.nr`. All 7 tags are compiled into some artifact.
- Nargo crates: `elixir_miner`, `elixir_spike` (`packages/contracts/*/Nargo.toml:2`, workspace members `packages/contracts/Nargo.toml:2`), `elixir_work`, `elixir_work_lib` (`packages/work-circuit/crates/*/Nargo.toml:2`, workspace `packages/work-circuit/Nargo.toml:2`); `sweep_1024`/`sweep_4096` depend on `elixir_work_lib`; `verify_w` does not. `use elixir_work_lib::` in `elixir_miner/src/{main.nr:22, ticket.nr:2, launch.nr:9}`, `elixir_spike/src/{main.nr:18, ticket.nr:2}`, `work-circuit/crates/{elixir_work,sweep_1024,sweep_4096}/src/main.nr`.
- Contract names: `pub contract ElixirMiner` (`elixir_miner/src/main.nr:14`), `ElixirSpike` (`elixir_spike/src/main.nr:10`, self-ref line 92); `ElixirMiner` appears 60× in `elixir_miner/src/test/mod.nr` including `env.deploy("ElixirMiner")` string literals (lines 42, 134, 151, 292, 376…).
- Artifact names `elixir_miner-ElixirMiner.json`, `elixir_spike-ElixirSpike.json`, `elixir_work.json` and their loaders: `miner-core/src/artifacts.ts:9–10`; `web-miner/src/chain.ts:38`; `web-miner/src/prover.worker.ts:24`; `web-miner/scripts/copy-artifacts.ts:11–12`; `deploy/src/deploy.ts:112`; `deploy/scripts/launch.ts:57`; `deploy/scripts/epoch-stats.ts:30`; `deploy/browser/main.ts:74,104`; `deploy/scripts/spike-browser.ts:38–41`; `deploy/scripts/spike-claim.ts:77,131`; `contracts/scripts/gates.ts:11`; `work-circuit/scripts/{sweep,determinism,wasm-prove,ticket-cost}.ts` (default crate arg `'elixir_work'`); `work-circuit/scripts/mutation.ts:26,78–82`; `work-circuit/scripts/export-vk.ts:10,41–42` (writes `vk.nr` into both contracts).
- VK: `W_VK`/`W_VK_HASH` embedded (generated) in `work-circuit/crates/verify_w/src/vk.nr:119`, `contracts/elixir_miner/src/vk.nr:119`, `contracts/elixir_spike/src/vk.nr:119`, `work-circuit/src/generated/vk.ts:119`; current `0x09fdc6464b84a29273e3de10fa5cfadd3c257a66d59630fc499679680b33fb53` — changes with the `DOM_WORK` tag.
- Fixtures/vectors: `work-circuit/fixtures/vectors.json` (written by `miner-core/scripts/pin-vectors.ts`; `deployDomain`, `secretCommitment`, `nullifier`, `nextSeed`, `digest/low128` change; `retarget` doesn't); `work-circuit/fixtures/elixir_work/{proof,public_inputs,vk,vk_hash}` must be re-proved (witness from `crates/elixir_work/Prover.toml` uses placeholder `domain = "1"`, so the witness is unchanged but the circuit is not). `proof-layout.json`/`layout-manifest.ts` are domain-independent (verified). Consumers that fail until regenerated: `work-circuit/src/vk-pinning.test.ts:6,25`, `miner-core/src/vectors.test.ts:8,45`, `miner-core/src/proof.test.ts:6`. Docs citing the old hash: `implementations-plan/elixir-core/{lessons/phase-1.md:13, spike-results.md:17, audit-fable.md:60, plan.md:53,141,162,175,267,298}`.
- Deployment records: `deployments/testnet.json` (immutable history of the live Elixir deployment: keep, don't edit); `docs/deployments.md` lines 6–7, 20–23, 33, 49–56, 66.

## 2. Package / workspace names

`package.json` `name`: root `elixir`; `@elixir/contracts`, `@elixir/deploy`, `@elixir/miner-core`, `@elixir/web-miner`, `@elixir/work-circuit`. `bun.lock` embeds them (lines 18, 22, 44, 59, 106, 375–383) and `bun install` rewrites it. No `@elixir/*` imports anywhere (cross-package imports are relative); `--filter` patterns are path globs; tsconfig/vite aliases only `@/* → ./src/*`.

## 3. Env vars, CLI, storage keys, markers

`ELIXIR_PROFILE` (`scripts/params-codegen.ts:13`), `ELIXIR_DEPLOYER_SECRET` (`deploy/src/deploy.ts:4,5,182–187`), `ELIXIR_DEPLOY_SALT` (`:5,189`), `ELIXIR_DEPLOY_FORCE` (`:6,192,194`; `docs/deployments.md:23`), `ELIXIR_LAUNCH_AT` (`:5,196–197`; `docs/deployments.md:7`), `ELIXIR_REQUIRE_TOOLCHAIN` (`scripts/run/toolchain.test.ts:15`; `.github/workflows/{contracts.yml:61, work-circuit.yml:58}`), `ELIXIR_AGENTS_DIR` (`scripts/run/registry.ts:8,13`; `registry.test.ts:8`), `ELIXIR_NODE_VERBOSE` (`isolated-node.ts:32,166`), `ELIXIR_RUN_ID` (`isolated-node.ts:233`; `agent.sh:3`), `ELIXIR_E2E_TARGET` (`web-miner/e2e/run-setup.ts:74`), `VITE_ELIXIR_MINER/TOKEN` (`web-miner/src/vite-env.d.ts:6–7`, `config.ts:17–18`, `.env.production:5–6`, `e2e/run-setup.ts:36–37`, `README.md:16`). localStorage `elixir.connection` (`config.ts:12`); IndexedDB `elixir-pxe-${ns}` / `elixir-wallet-${ns}` (`wallet.ts:30–31`); wrangler project `elixir-web-miner` (`wrangler.jsonc:4`, changes the pages.dev URL); runId prefix `elixir-` (`isolated-node.ts:168`); tmpdir prefix (`registry.test.ts:6`); debug global `window.elixir` (`main.tsx:25,33`, `e2e/miner.e2e.ts:168`); `deployElixir` (`deploy/src/deploy.ts:90`; `e2e/run-setup.ts:16,75`; `miner-core/src/live.test.ts:19,102,278,346`). No tmux usage. `.aztecrc`, `toolchain.lock.json`, `bunfig.toml` clean.

## 4. CI

Workflow/job names are package-scoped (nothing to rename). Path filters referencing renamed files: `contracts.yml:32,56`, `work-circuit.yml:26`, `miner-core.yml:29–30` (`packages/contracts/elixir_miner/src/**`), `web-miner.yml:27`. Composite actions clean. `commitlint.config.ts` has no scope enum. Repo URL only in `implementations-plan/elixir-core/lessons/phase-0.md:15` (GitHub repo rename is out-of-band).

## 5. Docs / prose (case-insensitive counts)

`implementations-plan/elixir-core/plan.md` 15, `eli5.html` 10, `docs/pitch/elixir-pitch.html` 9, `recon.md` 7, `CLAUDE.md` 7, `docs/deployments.md` 6, lessons/phase-1 5, context.md 5, `docs/roadmap.md` 4, `packages/web-miner/README.md` 3, lessons/phase-5 3, `README.md` 3, others ≤ 2. UI strings: `web-miner/index.html:6` `<title>elixir miner</title>`, `App.tsx:24` `elixir miner`, `deploy/browser/index.html:5`. Balance/reward strings read `PARAMS.TOKEN_SYMBOL` dynamically (`WalletCard.tsx:35`, `controller.ts:267`). `recon.md:16` mentions an external prototype `harsh-elixir` (don't rename).

## 6. Tests asserting names

`web-miner/e2e/miner.e2e.ts:60,66,78,81` (`/^0 tELX$/`, `/^4 tELX$/`, `/^8 tELX$/`), `:168` (`window.elixir`); `miner-core/src/live.test.ts` (`deployElixir`); `retarget.test.ts:100` (prose "ELX per hour"); `deploy/scripts/spike-claim.ts:87–88` and `spike-browser.ts:135–136` deploy a token with hardcoded `'Elixir', 'ELX', 18`. No test pins a page title.

## 7. Must NOT change

`@aztec/*`, `@aztec-foundation/aztec-standards`, `token_contract` / `TokenContract` / `token_contract-Token.json` (`contracts/scripts/token-artifact.ts:13`, `web-miner/scripts/copy-artifacts.ts:13–16`), `SponsoredFPCContract` / `SPONSORED_FPC_SALT` / `SponsoredFeePaymentMethod`, upstream git deps and tags, node/installer URLs, `AZTEC_NODE_URL` / `L1_RPC_URL`, `.aztecrc`, `toolchain.lock.json`.

## Summary

| category | files | risk |
|---|---|---|
| protocol-level | ~47 | regenerates VK/fixtures/vectors; new class ids; token metadata is a constructor arg → new deployment |
| package names | 6 + lockfile | mechanical |
| env/storage/CF names | ~17 | breaks existing `.env`, IndexedDB data, pages.dev URL |
| CI filters | 4 | silent non-triggering if missed |
| docs | ~25 | cosmetic |
| name-asserting tests | 6 | fail loudly (good tripwires) |

Recommended order: edit params (tags, seeds, token strings) → codegen → compile contracts + work circuit → re-prove the fixture → `export-vk` → `pin-vectors` → codegen again (vectors.nr) → `spike:manifest` → `contracts:test` + `bun test` (fix the 6 tripwires) → package names + `bun install` → env vars / storage keys / wrangler name → CI filters → docs → fresh deploy → new `deployments/<profile>.json` (old testnet.json stays as history).
