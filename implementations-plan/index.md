# Implementation plans

- [elixir-core](elixir-core/plan.md) — approved 2026-09-03, GO after Phase 1; Phases 0–5 ✓ (testnet deployed, 2 h soak reported); stacked PRs A1 → A2 → A3 open, unmerged — proof-of-proving mining mechanism: proof-hash lottery, Bitcoin-style difficulty, private mint; contracts + miner core + web miner. Results: `elixir-core/spike-results.md`. Contract hardening pass (2026-09-04, codex loop converged, launch gating / claim TTL / recipient-bound commitment / rollup-version domain): `elixir-core/audit-hardening.md`. Lessons: `elixir-core/lessons/phase-0.md` … `phase-5.md` (Phase 3 records the closing-claim gas defect the concurrent burst found; Phase 4 the pinned-CRS interceptor and the Playwright/Bun setup split).
- [yacana-surfaces](yacana-surfaces/plan.md) — approved 2026-09-05 (deep: 3 parallel plans, contradiction check, double audit, final codex pass); arcs 0–4 implemented 2026-09-05/06 (P0.1–P4.2 ✓; the per-arc codex loops and the final cross-arc pass converged; stacked PRs #5 → #9 merged 2026-09-06 as `c59b5c4`; follow-up: the site as a Worker with static assets; the first production deploy is the owner's `bun run site:deploy`) — rename Elixir → Yacana (new VK, fixtures, testnet redeploy), then `packages/ui`, the M1 web-miner redesign (passkey-first keys, vault, lost-race recovery by PXE reset, private/public withdraw), `packages/web-stats` (browser-side storage reads, slot table), `packages/web-landing` (H3 hero, money section, in-page demo proof) and `packages/site` (one Cloudflare Pages origin `yacana.network`, Git-integration builds). Recon: `yacana-surfaces/recon.md`; audits: `yacana-surfaces/audit-codex.md`, `audit-fable.md`. Lessons: `yacana-surfaces/lessons/phase-0.md` … `phase-4.md` (phase 2 records the arc-2 codex loop's five rounds and the PXE issue #25418; phase 4 the Cloudflare `_redirects` probe: wildcards shadow assets, exact sources to directory targets do not).
- [yacana-second-pass](yacana-second-pass/plan.md) — drafted 2026-09-07 (mid: competing outline, codex + fable audits, final codex pass), approved 2026-09-07, implemented 2026-09-08, in review as the stack #20 → #21 → #22 (stack #23) — the three surfaces to the owner's canvas picks: landing diet with a live hero tile and no demo, stats with a lead chart and a capped table, cockpit with a calm loop and the claim in the rail, one Send sheet, Sign out, explorer links, "account" not "key". Recon: `yacana-second-pass/recon.md`; audits: `audit-codex.md`, `audit-fable.md`; lessons: `lessons/phase-{0,1,2,3}.md`; renders: `shots/arc-{1,2,3}/`.
- [yacana-fidelity](yacana-fidelity/plan.md) — approved 2026-09-06 (light: codex reject → conditional approve, owner accepted the wallet-origin ask); implemented 2026-09-06/07 (P1.1–P3.3 ✓; the three arc loops and the final cross-arc pass converged — arc 3 and the cross-arc pass took four rounds each; stacked PRs #14 → #15 → #16 (stack #17) squash-merged 2026-09-07 as `4846519` and deployed as Worker version `c18941c9`) — the three surfaces to the binder's measured frames (M1 cockpit grid, L2 landing frames, A1 observatory grid), the stats charts on Observable Plot (tooltips, annotations, cross-fade on new epochs), a fixture-only screenshot gate in the stats PR gate. Recon: `yacana-fidelity/recon.md`; audit: `yacana-fidelity/audit-codex.md`. Lessons: `yacana-fidelity/lessons/phase-1.md` … `phase-3.md` (phase 1 records the dim-ink contrast decision and the render browser polluting the memory spec; phase 2 the difficulty-label artefact and the one-e2e-run-per-worktree rule; phase 3 the Plot facts, the rootless-Docker `--user` trap and the screenshot gate's three deliberate regressions).
- [presto-mine](presto-mine/plan.md) — **completed 2026-09-09** (PR #28 squash-merged as `27264a0`); approved 2026-09-09 (light: recon, one codex audit → rework, a resumed second pass → approve with changes, both folded in; the UI approved on a design canvas, Plan A of two); implemented 2026-09-09 (P1–P4 ✓, three codex fix-loop rounds to convergence) — the miner proves W through Presto, the visitor's native prover: the prover falls back to WASM inside itself and verifies a native win against the job's own inputs, the fetch guard admits exactly Presto's URLs, the billboard when absent and the miner's row when in the way, the ✦ suffix / `native` / dimmed slider after the first native proof, a headless Presto in the e2e lane and CI. Recon: `presto-mine/recon.md`; audit: `presto-mine/audit-codex.md`; canvas: `presto-mine/canvas/`; lessons: `presto-mine/lessons/phase-{1,2,3,4}.md` (phase 1: the native `bb` path, bb.js throwing on a non-canonical element, suites sharing one guard; phase 2: the LNA permission check; phase 3: the lane, and the CI-only balance-sync allowance; phase 4: the three codex rounds — per-proof provenance, a Start that brings native back, a download row that outlives the download).
- [e2e-lanes](e2e-lanes/plan.md) — approved 2026-09-10, implementing: P0 ✓ (arc A, PR #30), P1 ✓ (proving 30% of test time), P2 ✓ (three shards, 14.7 min slowest), P4 not built, P3 ✓ (the replay lane, ~2 min on pull requests); P5 next (mid: recon, a competing outline, dual audit → both reject, rework, a fresh pass → reject; a fourth read against the real CI run → conditional approve, its corrections codex-reviewed three rounds → conditional approve, conditions folded in) — the miner's browser suite faster without proving less: measure first, shard by named files, a record-and-replay lane on every push cloned from the stats screenshot gate, and the proverless switch last and conditional. Carries two findings worth having on their own: `YACANA_SITE_MODE` is cast without validation, and the isolated network has never verified a transaction proof. Recon: `e2e-lanes/recon.md`; audits: `e2e-lanes/audit-codex.md`, `audit-fable.md`.
- [yacana-third-pass](yacana-third-pass/plan.md) — **completed 2026-09-09** (approved 2026-09-08; mid: competing outline, codex ×6 rounds + fable audits; owner: senders dropped, live node switch; every phase ✓, the three arc loops and the cross-arc pass converged, PRs #24 → #25 → #26 as stack #27, awaiting the owner's merge) — the node as a setting (any https node, a banner when it throttles), the stats skeleton in two beats, the miner's dull signed-out cockpit with a sign-in modal and a progress modal, the strip's 48-cell window over a map of every epoch, the senders card out of the wallet, the pop-out's measured margin, the money table's last rule. Design canvas: Yacana Third Pass Proposals. Recon: `yacana-third-pass/recon.md`.
