# Phase 0 lessons — planning

- 2026-09-03 · Recon (2 sonnet Explore agents): aztec-nr 5.2.0 capability map; recursion/bb.js/aztec-standards. Load-bearing finding: chonk/README.md puts a naive in-circuit UltraHonk verifier above 512K gates; bb.js WASM CRS default 2^19. Became Phase 1's go/no-go.
- 2026-09-03 · Tier: rubric said `deep` (novelty, irreversibility, security all HIGH); user chose `mid`. Recorded in plan.md.
- 2026-09-03 · Probe (scratch, not committed): `verify_honk_proof_non_zk` costs 681,980 gates as a plain Ultra circuit but 19,923 gates inside an Aztec private function under `bb gates --scheme chonk` (baseline private fn 5,469). Aztec function bytecode must be extracted from the contract artifact (`functions[]` named `__aztec_nr_internals__<fn>`) into a program-shaped JSON; `--scheme ultra_honk` rejects it (CallData/ReturnData blocks need MegaCircuitBuilder). Turned plan Inferences I1/I2 into Fact 11; the spike now measures wall-clock and ECCVM headroom instead of gate count.
- 2026-09-03 · Probes C/D: `aztec-nargo execute` solves the recursion circuit with an all-zero proof (simulation never checks `recursive_aggregation` → TXE cannot reject bad proofs; real-proving tests mandatory). Work circuit: ≈74 gates per Poseidon2 step; native `bb prove` on M4 Pro: 0.34 s (1024 steps, 76k gates), 0.63 s (2048, 152k), 1.0 s (4096, 303k). `bb prove` needs `-k <vk>` from a prior `bb write_vk`; without it it fails with "Unable to open file: ./target/vk".
- 2026-09-03 · Probe E: binary UltraHonk proof = 410 × 32-byte big-endian fields (13,120 bytes); `--output_format` only accepts binary|json in bb 5.2.0. Flipping the lowest bit of each field and re-verifying (~15 s for 410 verifies) fails every time → zero unconstrained fields; plan §3.3 simplified to hash the whole proof.
- 2026-09-03 · Codex round 1 (gpt-5.6-sol xhigh, session 01a0679c…): **reject**. Blocking: (1) full-proof-per-ticket invariant unproven; (2) time-based epochs + ÷4 clamp over-issue ~3,000 claims under a 100× shock, grace window rewards withholding; (3) GO/NO-GO thresholds unsafe. Verified its factual claims (bb.js recursive-artifacts stub, Sumcheck.md uniform virtual rounds, aztec-standards auth_contract = ARC-403 hook). Plan rewritten to v2: count-capped epochs (N claims) + T_MAX escape hatch, no grace, deployment-bound inner PI, full-width nullifier, layout manifest + determinism/mutation/differential tests, redefined GO. Rejected: L1-portal benchmarking in Phase 1, recipient commitment in W, two-epoch lag. Fable-role audit: 3× HTTP 529 (fable, opus, opus) — relaunched on fable against v2.
- 2026-09-03 · Codex round 2 (resumed): **conditional approve**. Conditions: add `out` to claim/verify_ticket; early-abort ≥ 90% + per-phase recursive differential as hard Phase 1 gates; name the ECCVM capacity headroom is measured against; exact overflow-safe retarget arithmetic; wording: cap bounds accepted claims not submitted txs, issuance "at most" N×R, wall-clock spikes during catch-up; 1000× shock converges in ≤ 5 epochs (log₄); escape-hatch seed predictability accepted explicitly; digest listed as public; parity fixtures for every domain-separated hash. All adopted → plan v3.
- 2026-09-03 · Fable-role audit (model fable, 4th attempt succeeded): **conditional approve**. New findings beyond codex: non-ZK UltraHonk's 4 disabled sumcheck rows (sigma = id) allow a same-witness re-proof reusing the witness MSMs (~½–⅔ cost per ticket) → guarantee reframed as a measured lower bound; ECCVM is a fixed 2^15-row circuit, one verifier ≈ 800–900 rows; wallet-sdk has no message signing → per-epoch random secret; escape-hatch formula always clamps to ×4 → stated as ×4 and seed mixes last_digest; retarget must be tested at launch targets ~2^122; verifier inline by default (self-call = extra folding step); fee economics + mainnet fee path added as Ask 10; soak ≥ 24 h wall-clock stated. All six conditions adopted → plan v4. Note: my first python edit pass printed "applied" but never wrote the file (no write call) — always end edit scripts with an explicit write and verify with a diff/grep.
- 2026-09-03 · Codex final pass round 1 (fresh session 01a067da…, first attempt OK): **reject**. Critical: circular constructors (miner takes token, token takes miner) are a fixed point — Aztec addresses commit to constructor args; fixed with a token-less miner constructor + one-shot deployer `bind_token`. High: count-close elapsed unbounded when nobody rolls (cap at T_MAX); Field-division check needs range-constrained q; `last_digest` adds no entropy to roll seeds (accepted, simulated); genesis premine (accepted, bounded to 96 ELX); "lower bound" → measured attack cost; fee gate in fee-asset units; lying-RPC honesty; binaries/WASM/Actions pinned by hash/SHA. Plan v5. Lessons: I had carried the prototype's salt-precompute deploy idea without checking address derivation; and a python edit script ran from a drifted cwd — always use absolute paths in edit scripts and verify the write.
- 2026-09-03 · Codex final pass round 2 (resumed): **conditional approve** → gate verdict. Conditions: `token` must be `PublicImmutable` (PublicMutable is not private-readable in 5.2.0, `public_mutable.nr:10`) — its init nullifier doubles as one-shot binding; overflow-safe ceil/×4 helpers + `EXPECTED > 0` assert; replace remaining "lower bound" / "caps chain load" / "fee vs REWARD" overclaims. All adopted → plan v6. Total audit trail: codex R1 reject, R2 cond. approve; fable cond. approve; final codex R1 reject, R2 cond. approve.
