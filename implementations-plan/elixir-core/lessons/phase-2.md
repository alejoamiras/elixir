# Phase 2 lessons — contracts (homelab, 2026-09-03)

**Gate (plan §6)**: `bun run contracts:compile && bun run contracts:test && bun test packages/miner-core/src/retarget*` exit 0; lint. Result at close: **passed** — compile OK, TXE 20/20, retarget simulator 9/9, lint clean. After the A2 and cross-arc audits the same commands give TXE 32/32 (incl. the 7 generated cross-language vector tests) and 10 simulator tests.

- Parameters: `elixir.params.json` (profiles `testnet`, `mainnet`) → `scripts/params-codegen.ts` writes `packages/contracts/elixir_miner/src/params.nr`, the work crate's `params.nr` (CHAIN_LEN) and `packages/miner-core/src/generated/params.ts`; `bun run codegen` chains it before the VK export so a CHAIN_LEN change also re-exports the VK. Testnet profile: N = 4, 300 s epochs, T_MAX 1200, initial target 2^124, 4 tELX reward.
- Retarget in Noir without a u256: `q = ⌊target·actual/EXPECTED⌋` cannot be computed in u128 (the product reaches 2^147), so the hint splits `target = a·d + b` and returns `q = a·actual + ⌊b·actual/d⌋` as a Field; constrained by `assert_max_bit_size::<130>` on q, `::<64>` on r, `r < EXPECTED` and the field identity. `Field::lt` does the clamp comparison; `q as u128` is only used on the in-range branch. Six native `#[test]`s (`aztec-nargo test`) cover on-schedule, ÷2, ⌈/4⌉ clamp, ×4 at T_MAX, saturation at 2^128−1, odd durations at 2^118/2^126 and the extrema of the bound helpers.
- TXE: a dependency contract's artifact must sit in the package's `target/` (`token_contract-Token.json`), or the TXE worker crashes with ENOENT — `pretest` copies it from `@aztec-foundation/aztec-standards`. Deploy path for a dependency is `env.deploy("@token_contract/Token")`. Token string args are `str<31>`. Since the TXE accepts any proof bytes, each test claim uses a fabricated proof ground (unconstrained, ≈64 Poseidon2 evaluations) until its ticket is below the target, which also gives each claim a distinct nullifier; the first test (`txe_accepts_a_fabricated_proof`) pins the plumbing-only nature. 20 tests green: bootstrap (non-deployer bind, one-shot bind, claim before bind), mint amount, only_self, replay, losing ticket, Nth-claim close with exact retarget and seed chaining, late count close capped at T_MAX (×4), roll before/after T_MAX with `last_digest` in the seed, claim after close, constants view. Not covered here (Phase 3, real prover): stale-claim-before-mint ordering and public-effects inspection.
- A quirk: a test whose fixture search loop was inlined in the test body failed with "Function Artifact does not exist" from the TXE; moving the loop into a helper (`losing_proof`) fixed it.
- Simulator (`packages/miner-core/src/retarget.test.ts`, mean-field): 10×/100×/1000× shocks converge in 2/4/5 epochs; ÷100 collapse recovers via three rolls only with headroom below the 2^128 ceiling — from 2^122 the target saturates at "every proof wins" and a fleet at H0/100 still cannot make N claims per hour, so the convergence claim holds for launch targets ≤ ~2^120 at mainnet N = 24 (recorded for the threat-model doc). Withholding, premine and roll-seed bounds and the 200-winner burst are asserted as state properties.
- Deploy script (`packages/deploy/src/deploy.ts`): bootstrap per §3.4, sponsored FPC, deployer = initializerless Schnorr account from `ELIXIR_DEPLOYER_SECRET` with a fixed salt (same secret → same address), writes `deployments/<profile>.json`.
