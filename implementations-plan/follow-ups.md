# Follow-ups

Open items no active plan owns. Delete entries as they resolve.

- **Plan-folder hygiene migration**: `implementations-plan/.gitignore` now ignores `audit-*.md`, `plan-*.md`, `_*.md` and `eli5.html`, but 27 such files from earlier plans are still tracked (`git ls-files -ci --exclude-standard -- implementations-plan`), six of them carrying absolute local paths, and `index.md` links to several. Untracking them and repairing the index links is the owner's call; history keeps them either way.
- **One toolchain pin resolver** (from monorepo-layout): `scripts/run/toolchain.ts` (later `tools/localnet`) and `work-circuit/scripts/toolchain.ts` both resolve the pinned Aztec binaries; `portal` and `l1-deploy` get `aztec-forge` through the first. Merge them below `tools`.
- **Pin Node** (from monorepo-layout): nothing pins it in the repo, CI or Workers Builds, and Vite's config loader hands bare specifiers to the ambient Node. Four config-time imports stay relative to avoid depending on it.
- **Browser reachability check** (from monorepo-layout): walk the import graph from each app's page and Worker entries and refuse `tools`, `scripts/` and Node-only modules. `web-kit` is dual-environment and `siteVite` polyfills Node, so a wrong import need not fail the build.
- **`deploy/src` imports** (from monorepo-layout): it handles the L1 key and the deployer secret and sits in the layer that may import anything; forbid it from importing `harness` and `localnet`.
- **`bridge-vectors.json` under `protocol/`** (from monorepo-layout): `portal`'s Foundry tests read it upward from `packages/bridge/fixtures`.
- **The `miner-core` barrel** (`src/index.ts`) has no importer outside the package.
- **`compile` scripts call `aztec` / `aztec-nargo` from `PATH`** (`contracts`, `work-circuit`), not the pinned path.
- **`assemble.test.ts` writes into the real `packages/site/dist`** (from monorepo-layout): two cases assemble into `PRODUCTION_OUT`, so every `bun test` leaves a stub `stats/assets/index-abc.js` in a build that is there. Assemble into a scratch folder with the production contract asked for explicitly.
- **The cockpit's memory bound on a many-core host** (from monorepo-layout): "three power changes … memory stays bounded" fails on a 192-thread machine at the baseline commit too (+900 MiB against 300). Decide whether the bound or the slider's ceiling is the thing to change.
- **`node-guard.test.ts` reaches bb.js's browser WASM loader by path** (from monorepo-layout, codex arc 2 round 2): `../../../../node_modules/@aztec/bb.js/dest/browser/…/fetch_code/browser/index.js` is not an exported subpath, so the test resolves through the hoisted root, not its owner. Pre-existing; an isolated linker would fail it. Either bb.js exports the module or the test loads the WASM another way.
- **`web-kit`'s project mixes browser code and Bun scripts** (from monorepo-layout, codex arc 4): `packages/web-kit/tsconfig.json` checks `src/browser/*` under `bun` types, so a `Bun.file(…)` in a browser module passes there and is caught only by the apps that import it. A `browser` sub-project on `vite/client` (or `lib: DOM` without `bun`) would make the kit's own typecheck refuse it. Pre-existing: the same files sat under the root's `bun` project before.
- **`bun.lock` names the root workspace `elixir`** (from monorepo-layout, codex arc 5): line 6, left by the protocol rename; `bun install` does not rewrite it. Cosmetic, exempt from the rename guard; regenerate the lock's root entry when the lock is next rebuilt on purpose.
- **Two Presto start/stop tests are load-sensitive** (from monorepo-layout, cross-arc round 3): `apps/web-miner`'s "Start and Retry against Presto" suite failed twice at host load ~540 (one at the 5 s default timeout) and passed at ~240. Give the probe a fake clock or a wider budget.
- **Forms the boundary and layout guards do not read** (from monorepo-layout, codex arc 1 round 5): a Vitest helper reached through destructuring or an alias, `createRequire(…)('@yacana/…')`, an interpolated template import; a test importing both runners or neither. None exists today; refuse them when the idiom first appears.
- **`aztec-txe` can die mid-run on a CI runner** (from monorepo-layout, Delivery): one `contracts` run lost the TXE to an uncaught `Napi::Error` and every later test failed on `client error (Connect)`; a rerun passed. If it recurs, make `contracts:test` fail fast on a dead TXE so the log names the crash, not forty connection errors.
- **yacana-feedback-pass · F6, the pop-up's horizontal scrollbar: not reproduced.** Three attempts in Chromium 151 (the opening e2e on the old stepper, every right-column string in the dialog's box, every signed-out account screen × six window sizes with 17 px classic scrollbars) all fit. The stepper's right column now wraps and the opening e2e guards that mechanism, but what the owner saw is still unexplained. Needs from the owner: a screenshot, which pop-up (the account dialog, a transaction dialog, or the pop-out window), browser + OS. See `yacana-feedback-pass/lessons/phase-2.md`.
