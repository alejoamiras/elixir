{
  "name": "@yacana/contracts",
  "version": "0.0.0",
  "private": true,
  "description": "Aztec contracts: yacana_miner plus the aztec-standards token as a git dependency",
  "type": "module",
  "scripts": {
    "compile": "aztec compile --workspace",
    "pretest": "bun scripts/token-artifact.ts",
    "test": "aztec test && aztec-nargo test --package yacana_bridge_hashes"
  }
}
