{
  "name": "@yacana/work-circuit",
  "version": "0.0.0",
  "private": true,
  "description": "The Noir work circuit W (Poseidon2 chain), its VK, VK hash, proof-layout manifest and fixture proofs",
  "type": "module",
  "scripts": {
    "compile": "aztec-nargo compile --workspace",
    "export-vk": "bun scripts/export-vk.ts",
    "spike:determinism": "bun scripts/determinism.ts",
    "spike:manifest": "bun scripts/layout-manifest.ts",
    "spike:mutation": "bun scripts/mutation.ts",
    "spike:sweep": "bun scripts/sweep.ts",
    "spike:ticket-cost": "bun scripts/ticket-cost.ts",
    "spike:wasm": "bun scripts/wasm-prove.ts",
    "test": "bun test"
  },
  "dependencies": {
    "@aztec/bb.js": "5.2.0",
    "@aztec/noir-noir_js": "5.2.0"
  }
}
