name: portal

on:
  workflow_dispatch:
  pull_request:

permissions:
  contents: read

jobs:
  changes:
    uses: ./.github/workflows/_changes.yml
    permissions:
      contents: read
      pull-requests: read
    with:
      filters: |
        relevant:
          - 'packages/portal/**'
          - '!packages/portal/**/*.md'
          - 'packages/bridge/fixtures/**'
          - 'packages/deploy/scripts/l1-deploy.ts'
          - 'tools/localnet/**'
          - '.aztecrc'
          - 'toolchain.lock.json'
          - 'package.json'
          - 'bun.lock'
          - '.github/workflows/portal.yml'
          - '.github/actions/**'
          - '.github/workflows/_changes.yml'

  test:
    name: Build + test + deploy on anvil
    needs: changes
    if: needs.changes.outputs.relevant == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
      - uses: ./.github/actions/setup-bun
      - uses: ./.github/actions/setup-aztec
      - run: bun test tools/localnet/src/toolchain.test.ts
        env:
          YACANA_REQUIRE_TOOLCHAIN: '1'
      - run: bun run portal:build
      # The committed ABIs are what the apps read: they must match the build.
      - run: bun packages/portal/scripts/abi.ts && git diff --exit-code packages/portal/abi
      - run: bun run portal:test
      - run: bun packages/deploy/scripts/l1-deploy.ts --anvil
